import _ from 'lodash';

const initState = {
    isLoading: false,
}

const defaultReducer = (state = initState, action) => {
    switch(action.type){

        default:
            return state;
    }
}

export default defaultReducer;