const initialState= {
    theme: null,
    darkMode: null,
}

const ThemeReducer = (state = initialState, action) => {
    switch(action.type){
        default:
            return state;
    }
    
};

export default ThemeReducer;